"use server";

export async function addMatch(_formData: FormData) {
  void _formData;
  return { ok: false, message: "Scoreboard er ikke aktiveret i den lokale simple version endnu." };
}

export async function deleteMatch(_formData: FormData) {
  void _formData;
  return { ok: false, message: "Scoreboard er ikke aktiveret i den lokale simple version endnu." };
}

export async function approveMatch(_formData: FormData) {
  void _formData;
  return { ok: false, message: "Scoreboard er ikke aktiveret i den lokale simple version endnu." };
}
